# MODEL-SELECT — picker→catalog→tier/filter/joinable→admission POST→chat envelope

> Version: live CLI 0.0.194 vs static pin 0.0.193. No tokens/hosts.

## Picker (cli/src/components/freebuff-model-selector.tsx)
- Store selectedModel (:248-249, default recommended hero) + focus cursor focusedId (:786) → session snapshot arrives (useFreebuffSessionStore session, :257-260; accessTier full|limited, :258-260; freebucks block presence=meter gate, :292-296; planRequired via freebuffPlanRequired(model,hasPaidSubscription,freebucks), :305-309).
- availableModels=getFreebuffModelsForAccessTier(accessTier,hasPaid)+sortModelsByPrice when metered (:323-333) → offers=getLimitedModelOffers(session) filtered by isSupported (:344-350) → meterFor=getFreebuffModelMeter{freebucks,quota:rateLimitsByModel[model]} (:378-400) → premiumSectionQuotas header=shared pool (:407-424) → recommendedModel=getRecommendedFreebuffModelId(accessTier,{premiumExhausted}) else first joinable (:560-567).
- isJoinable=isFreebuffModelAvailable(now)+offer.userRemaining>0+active-session pin+!planRequired+meter.canStart (:536-557); isPricedOut/pressable gate Enter audibility (:614-628). Sections: metered=flat metered list (:826-830), limited=unlabeled (:813-816), full=PREMIUM+UNLIMITED (:831-847), offer leads LIMITED TRIAL both states (:861-874). Render order standalone hero+sections (:878-884), nav=rows+TOGGLE+referral (:888-895). Keyboard Tab/arrows focus, Enter/Space commits (:1244-1305). pick(modelId): intent=rowIntent(freebucks,model,active) (:579-595): allow→start, confirm/paywall first Enter asks (pendingAsk), second commits; paywall→safeOpen(plans URL) (:1192-1231) → startSession(modelId,walletSpend?) default startFreebuffSession (:240,1220-1225).

## Catalog (common/src/constants/freebuff-models.ts — single source)
- FREEBUFF_MODELS (served picker rows, [0] pinned DEFAULT_FREEBUFF_MODEL_ID), SUPPORTED_FREEBUFF_MODELS (admissible incl. retired/withdrawn for drain/coercion), FREEBUFF_WEB_ALL_MODELS (web-only), LIMITED_FREEBUFF_MODELS (limited-tier hero +1 row); ids in freebuff-model-ids.ts, entitlements in freebuff-model-entitlements.ts; availability via row availability + isFreebuffModelAvailable/isAvailableAt + resolveFreebuffModel fallback chain → FALLBACK_FREEBUFF_MODEL_ID. Proxy generated catalog backend/internal/modelcat/catalog_gen.go (from freebuff-models.ts+ids+entitlements+model-config); queries catalog_query.go + catalog_ladder.go; registry registry.go/registry_resolve.go/registry_refresh.go/parse.go.
- Display: line1 name+tagline+Reasoning/Images/NEW/TEST suffixes; line2 rowDetails price N Freebucks/hr (warn if balance<price, accent if first-tab list price) + warning + deployment/closed label + off-meter own-pool chip. PIN_MODEL has NO CLI equivalent (proxy-only slot:model pin: backend/internal/config/pin_model.go).

## Admission POST (one send)
- POST /api/v1/freebuff/session/admission, no body/no Content-Type, headers Authorization Bearer `<redacted>` + Bun UA + x-freebuff-model:`<id>` (if set) + x-freebuff-wallet-spend-limit:0 + x-fb-timezone:`<IANA>` + x-freebuff-first-tab-discount:0/1 (proxy mirror upstream/session.go:87-99; CLI callFreebuffSession + freebucksTimeZoneHeaders). Poll GET /api/v1/freebuff/session?instanceId= + compact merge every 30s±20% jitter (use-freebuff-session.ts:63,76-88).

## Chat send (one turn) → envelope codebuff_metadata
- selectedModel→template.model (prompt-agent-stream.ts:81,97) via getAgentStreamFromTemplate→aiSdkStreamParams{model,runId,clientSessionId(promptId per prompt),extraCodebuffMetadata:{freebuff_instance_id,freebuff_reasoning_effort?} only if IS_FREEBUFF&&!byok&&instanceId (use-send-message.ts:664-676), cost_mode free, fingerprintId (login only, not chat)} → SDK llm.ts getProviderOptions stamps codebuff_metadata{run_id,client_id:13-char base36 per-run repeated,trace_session_id per-run,freebuff_instance_id per-session,llm_step_number:String(n) per step,cost_mode,n?,cache_debug_correlation?,freebuff_reasoning_effort mirrored from top-level reasoning_effort} + provider{data_collection:deny,order?,allow_fallbacks?} + stream:true forced (proxy injectEnvelope chat.go:409-517; run.ts:722/822, llm.ts:112-122, run-agent-step.ts:1175-1177).
- POST /api/v1/chat/completions headers Authorization + User-Agent ai-sdk/openai-compatible/1.0.0/codebuff (chat.go:130, client.go:126-135) + optional x-freebuff-acting-user-id own-id only (chat.go:136-150); NO x-freebuff-model/instance headers on chat (chat.go:131-135). Agent-run lifecycle POST /api/v1/agent-runs {action:START,agentId,ancestorRunIds:[]}→runId, per-step chat calls, FINISH {completed|cancelled|failed} (upstream/session.go:361-370; runs/steps.go:77-80). Stream: PromptAiSdkStreamFn maxRetries:3 (prompt-agent-stream.ts:95); run-agent-step.ts onUsageReceived→onAgentUsageReceived{isRoot,agentId} (:564-571), onCostCalculated→creditsUsed (:1203-1205), addAgentStep completed/skipped/failed (:1217-1226); quota NOT from stream — next session GET poll refreshes rateLimitsByModel/freebucks/subscription.

## UNVERIFIED
- Server effort authority (resolveFreebuffReasoningEffort) behavior; live catalog rows at 0.0.194 (Solar swap/Mini 4/Space Bunny notes in docs/UPSTREAM-CLI.md:12-19 are 0.0.188-era).
- Exact served-model set deltas 0.0.193→0.0.194 (re-verify via capture + registry refresh).
