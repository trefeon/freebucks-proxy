# LOGIN-TUI — ordered screens→code→endpoint→persist

> Version: live CLI 0.0.194 vs static pin 0.0.193. No tokens/hosts. Vendor tip SHA UNVERIFIED (clone not fetched under read-only constraint; doc pin 0.0.188/40c75256f from docs/UPSTREAM-CLI.md:12-33 is reference only).

## Ordered trace
1. Launch: index.tsx:237 detects `login` command; :241 initializeApp (init-app.ts:9-33, background void getFingerprintId() prefetch :33); :244 setApiClientAuthToken(getAuthToken()); :247-250 `login` -> runPlainLogin, else TUI AppWithAsyncAuth effect :353-364 sets requireAuth=true when no token (credentials file else CODEBUFF_API_KEY, auth.ts:143-157), or requireAuth=false + hasInvalidCredentials=true when a token exists (validated by useAuthQuery).
2. TUI gate app.tsx:238-250 renders LoginModal when requireAuth && isAuthenticated===false && authStatus==='ok'.
3. Screen 1 prompt 'Press ENTER to login...' (login-modal.tsx:357-360); keys: ENTER -> fetchLoginUrlAndOpenBrowser (:110-133), c -> copy URL, ctrl+c -> exit (use-login-keyboard-handlers.ts:32-73).
4. Fingerprint getFingerprintId() (fingerprint.ts:152-157, process-cached promise): enhanced calculateEnhancedFingerprint (:84-129) from node-machine-id (:23-34) + systeminformation system/cpu/osInfo (:36-77) + shell + MAC list, sha256-base64url -> `enhanced-<hash>` (:123-128); failure -> legacy `codebuff-cli-<8 random>` (:135-138); sync legacy-only generateFingerprintIdSync (:223-225). Only the fingerprintId string is sent; raw hostname/MAC/serial stay local (folded into the hash).
5. generateLoginUrl (login-flow.ts:37-86): POST {LOGIN_WEBSITE_URL}/api/auth/cli/code body {fingerprintId} unauthenticated (codebuff-api.ts:516-523) -> {loginUrl, fingerprintHash, expiresAt} (:44-48). LOGIN_WEBSITE_URL = FREEBUFF_WEB_URL for IS_FREEBUFF builds (login/constants.ts:21-24; prod host const FREEBUFF_WEB_URL_PROD, dev loopback override).
6. use-fetch-login-url.ts:43-52 stores triple in login-store + isWaitingForEnter/hasOpenedBrowser, then safeOpen(loginUrl) (open-url.ts:29-63; skips headless Linux, checks powershell on WSL).
7. Screen 2 prompts (login-modal.tsx:366-471): 'Open this URL in your browser...' + wrapped URL + 'Press c to copy' + '[ Copy link (c) ]' + 'Waiting for login...' + remote-session tip 'Exit and run freebuff login instead'; variants: invalid-creds banner (:261-278), 'Loading...' (:306-318), 'Error: ... / Please restart the CLI' (:321-344).
8. Poll use-login-polling.ts:52-113 -> pollLoginStatus via 'modal': GET /api/auth/cli/status?fingerprintId&fingerprintHash&expiresAt unauthenticated (codebuff-api.ts:525-536); defaults intervalMs=5000 timeoutMs=5min (login-flow.ts:122-123); 401 = pending (silent), other non-ok warn, network error logged, all continue; success on data.user object (:183-189); LOGIN_TIMEOUT/LOGIN_ABORTED events.
9. Modal success: handleLoginSuccess (login-modal.tsx:148-163) -> useLoginMutation (use-auth-query.ts:190-204) saveUserCredentials THEN validateApiKey (GET id+email via getUserInfoFromApiKey -> /api/v1/me fields, :64-76) -> use-auth-state handleLoginSuccess (:97-125) identifyUser + LOGIN via 'modal' + resetCodebuffClient/resetChatStore/resetLoginState + isAuthenticated=true. Plain-command mirror (plain-login.ts:65-104): same poll via 'plain_command', saveUserCredentials, identify + LOGIN + flushAnalytics, exit 0; timeout/abort exit 1.
10. Persist saveUserCredentials (auth.ts:205-230): merge {default:{id?,name(null->''),email,authToken,fingerprintId?,fingerprintHash?}} (schema :14-29) into <configDir>/credentials.json (getCredentialsPath :45-47), file 0600/dir 0700, drops legacy chatgptOAuth (:189-200). Config dir (config-dir.ts:16-36): $FREEBUFF_CONFIG_DIR (must be absolute) else ~/.config/manicode[-<env>] (prod ~/.config/manicode). Logout (auth.ts:263-295): POST /api/auth/cli/logout {userId,fingerprintId,fingerprintHash} authed, then delete default key/unlink.
11. Proxy port: StartCLILoginWithFingerprint backend/internal/upstream/auth_login.go:125-185 (onboard rewrite :172-177); PollCLILogin :231-310 single-shot Done=false on 401/5xx/transport; pollForCompletion :416-443 5min loop; isolated random fingerprint :118-123; stable machine hash backend/internal/upstream/login/fingerprint.go.

## No typed onboarding form
- NO name/email/timezone/locale prompts in CLI — id/email/name arrive from server in the poll user object. Onboarding = SuggestedPrompts chips (suggested-prompts.tsx:30-46, three fixed labels) shown when IS_FREEBUFF && !hasSubmittedFirstPrompt (chat.tsx:155-156,1687-1713); markFirstPromptSubmitted -> hasSubmittedFirstPrompt:true in <configDir>/settings.json (settings.ts:77-78,341-352).
- Anonymous pre-login id = random UUID in <configDir>/analytics-id.json (anonymous-id.ts:26-55), aliased via identifyUser at login.
- Timezone sent per freebuff-session request as x-fb-timezone (freebucks-timezone.ts:17-25); ads device {os,timezone,locale} only to ads API (use-gravity-ad.ts:793-815); GitHub appears only as referral banner 'Signed up with Google? Connect GitHub to qualify' opening <site>/account (freebuff-referral-banner.tsx:561-567,666-667).

## UNVERIFIED
- Server code/status handler impl, exact loginUrl shape/query keys, browser OAuth UI, fingerprintHash derivation (candidate genAuthCode=sha256(secret+fingerprintId+expiresAt) common/src/util/credentials.ts:16-26), server expiry (client 5min vs 1h), expiresInMs field.
