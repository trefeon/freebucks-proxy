# ── VCS ──
.git

# ── third-party reference clones (dev only; shallow clones, ~253MB) ──
reference/

# ── local secrets / credentials ──
.env
.env.local
.env.*
!.env.example
!.env.full-example
config.json
config.*.json
!config.example.json
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx

# ── secret-adjacent locals (never in builder layers) ──
API_Keys*
API_KEYS*
api-keys*
api_keys*
*api-key*
*api_key*
*vps*key*
*vps*keys*
tokens.txt
credentials.json
creds.json
auth.json
.freebuff-session-state.json*
*.session-state.json
*.tmp
*.bak
*.exe~
/.drift-report.json

# ── local docker compose overrides ──
docker-compose.override.yml
docker-compose.*.local.yml
compose.override.yml

# ── runtime artifacts ──
dump/
# Local SQLite history must never bake into image layers.
/data/
*.log
*.db
*.db-*
*.sqlite*
*.shm
*.wal

# ── build outputs / dev binaries ──
dist/
!backend/internal/dashboard/dist/
out/
bin/
tmp/
uicheck/
freebucks-proxy
freebucks-proxy.exe
*.test
*.out
coverage.out
coverage.html
*.prof

# ── caches / editor / OS cruft ──
.ruff_cache/
**/node_modules
/main/
.idea/
.vscode/
Thumbs.db
Desktop.ini
.DS_Store

# ── agent scratch / tools ──
.agents/
.claude/
.antigravity/
.gemini/
.cursor/
.windsurf/
scratch/
.scratch/
*.scratch
*.patch
*.diff
skills-lock.json

# ── test results & coverage ──
coverage/
playwright-report/
frontend/playwright-report/
test-results/
frontend/test-results/
blob-report/
.nyc_output/

# ── local-only docs (gitignored, not repo content); public guides stay ──
devdocs/
docs/*
!docs/*.md
HANDOFF.md
*.handoff.md
plan.md
PLAN.md
*.plan.md
